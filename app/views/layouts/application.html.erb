<!DOCTYPE html>
<html>
  <head>
    <title>Blocipedia</title>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
  </head>

  <body>
    <div class="container-fluid">
      <div class="row">
        <div class="col-9">
          <%= render partial: '/layouts/alerts' %>
        </div>

        <div class="col-2 user_display">
          <% if user_signed_in? %>
            <div class="container user-display">
              <div class="row">
                <div class="diamond">
                  <%= user_gem_display %>
                </div>
                <div class="edit-user-link">
                  <%= link_to edit_user_registration_path do %>
                    <%= content_tag :span, current_user.email, :class => "user-link" %>
                  <%end%>
                </div>
              </div>
            </div>
          <% end %>
        </div>

        <div class="col-1 about-link">
          <% if user_signed_in? %>
            <%= link_to('Sign Out', destroy_user_session_path, :method => :delete) %>
          <% else %>
            <%= link_to('About Blocipedia', welcome_about_path) %>
          <% end %>
        </div>
      </div>
    </div>

    <%= yield %>
  </body>
</html>
